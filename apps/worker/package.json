{
  "name": "@unturf/unfirehose-worker",
  "version": "1.1.2",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch --include '../../packages/core/**/*.ts' --exclude '../../packages/core/**/*.test.ts' src/main.ts",
    "start": "tsx src/main.ts",
    "build": "echo 'worker: no build step (tsx runtime)'",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@unturf/unfirehose": "*"
  },
  "devDependencies": {
    "@unturf/unfirehose-config": "*",
    "@types/node": "^20",
    "tsx": "^4",
    "typescript": "^5",
    "vitest": "^4.0.18"
  }
}
